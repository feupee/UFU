#include <stdio.h>

    struct horario{
        int hora, minuto, segundo;
    };
    struct data{
        int dia, mes, ano;
    };
    struct compromisso{ 
        struct data;
        struct horario;
        char texto[50];
    };

int main(){

    //código a ser digitado

    return 0;
}